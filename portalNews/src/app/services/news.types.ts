export type New = {
    id: string
    title: string
    image: string
    created: string
    sectionId: string
    author: string
    section: string
} 

export type NoticeDetail = {
    title: string
    image: string
    created: string
    content: string
    author: string
    section: string
}